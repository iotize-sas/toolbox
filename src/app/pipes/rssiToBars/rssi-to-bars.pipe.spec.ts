import { RssiToBarsPipe } from './rssi-to-bars.pipe';

describe('RssiToBarsPipe', () => {
  it('create an instance', () => {
    const pipe = new RssiToBarsPipe();
    expect(pipe).toBeTruthy();
  });
});
